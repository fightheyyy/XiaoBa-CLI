# Live Benchmarks

Only live agent eval benchmarks live here.

Current root:

- `BaseRuntime/`: 11 Pet/IM runtime replay cases.

Removed roots:

- RoleArena
- EngineerCat
- ResearcherCat
- UserCat

Those can return only after they are rewritten as live agent eval cases with input, setup, replay, expected tool use, expected result, and verifiers.

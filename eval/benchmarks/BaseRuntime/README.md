# BaseRuntime Live Agent Eval

11 个 live Pet/IM runtime replay cases。

Run:

```bash
npm run eval:base-runtime
```

This benchmark evaluates current agent/runtime behavior from input request to tool use to visible result. It does not contain static historical trace regression.

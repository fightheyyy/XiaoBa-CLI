# Evaluation Benchmarks SPEC Proxy

Live benchmark source spec 由 [`../../../eval/benchmarks/SPEC.md`](../../../eval/benchmarks/SPEC.md) 维护。

保留本文件是为了让 benchmark supporting docs 收进 `docs/evaluation/`；真实 benchmark source 仍在 `eval/benchmarks/`。

# Evaluation Benchmarks PLAN Proxy

Live benchmark source plan 由 [`../../../eval/benchmarks/PLAN.md`](../../../eval/benchmarks/PLAN.md) 维护。

保留本文件是为了让 benchmark supporting docs 收进 `docs/evaluation/`；真实 benchmark plan 仍在 `eval/benchmarks/`。

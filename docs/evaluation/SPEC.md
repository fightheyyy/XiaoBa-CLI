# Evaluation SPEC Proxy

Live Agent Eval source spec 由 [`../../eval/SPEC.md`](../../eval/SPEC.md) 维护。

保留本文件是为了让 `docs/` 根目录按模块命名；真实 live agent eval benchmark 边界仍以 `eval/` 为唯一实现与维护入口。历史 trace replay 由 [`../trace-replay/SPEC.md`](../trace-replay/SPEC.md) 维护，不属于 `eval/` benchmark。

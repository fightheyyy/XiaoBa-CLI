# XiaoBa-CLI Architecture Spec

项目级总架构 spec 已迁移到根目录 [`../SPEC.md`](../SPEC.md)。

保留本文件是为了兼容历史链接。后续涉及整体架构、agent harness 边界、核心状态机、日志 schema、评测闭环和扩展规则的变更，都应该更新根目录 `SPEC.md`。

专题文档仍保留在 `docs/reference/`，但不承担项目级真相源职责。
